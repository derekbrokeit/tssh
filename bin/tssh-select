#!/bin/bash -l
# Copyright 2012 Derek Ashley Thomas

if [[ $COMP_TYPE != "local" ]] ; then
    echo "*** Please use osselect from a local computer, not $HOSTNAME"
    exit
fi

while true; do
    read -p "Server: " server
    if [[ ! -z $server ]] ; then
        tssh $server
    fi
done
